# Déploiement Dressify sur VPS via Docker Compose.
#   1) (optionnel) crée un fichier .env à côté de ce fichier avec tes clés VITE_*
#   2) docker compose up -d --build
#   3) l'app est servie sur http://VPS_IP:8080  (change le port si besoin)
services:
  dressify:
    build:
      context: .
      args:
        VITE_SUPABASE_URL: ${VITE_SUPABASE_URL:-}
        VITE_SUPABASE_ANON_KEY: ${VITE_SUPABASE_ANON_KEY:-}
        VITE_ANTHROPIC_API_KEY: ${VITE_ANTHROPIC_API_KEY:-}
        VITE_CLAUDE_MODEL: ${VITE_CLAUDE_MODEL:-claude-sonnet-4-6}
        VITE_OPENWEATHER_API_KEY: ${VITE_OPENWEATHER_API_KEY:-}
    image: dressify:latest
    container_name: dressify
    restart: unless-stopped
    ports:
      - "8080:80"
